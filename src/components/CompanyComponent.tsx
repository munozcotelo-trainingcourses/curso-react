import React from "react";

const CompanyComponent : ( props : Record<string, unknown> ) => React.ReactElement = ( props : Record<string, unknown> ) => {

    return (

        <React.Fragment>

            <h3>
                Company
            </h3>

            <div>
                Bla bla bla
            </div>

        </React.Fragment>

    );

}

export default CompanyComponent;
